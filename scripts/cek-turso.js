#!/usr/bin/env node
/**
 * TIDAK DIPAKAI LAGI.
 *
 * Bot sudah tidak memakai database (Turso maupun SQLite). Semua data kini
 * disimpan di file JSON — lihat src/utils/store.js.
 *
 * Untuk memindahkan data lama, gunakan:  npm run migrasi-json
 *
 * File ini boleh Anda hapus.
 */
console.log('Skrip ini sudah tidak dipakai. Bot kini memakai penyimpanan file JSON.');
console.log('Untuk migrasi data lama: npm run migrasi-json');
